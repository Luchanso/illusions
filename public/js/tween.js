var tween = new TWEEN.Tween(window)
    .to({ score: val }, 1000)
    .start();
// 
// requestAnimationFrame(animate);
//
// function animate(time) {
//     requestAnimationFrame(animate);
//     TWEEN.update(time);
// }
